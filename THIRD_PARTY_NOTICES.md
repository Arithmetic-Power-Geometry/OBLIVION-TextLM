# Third-party notices

OBLIVION TextLM does not bundle pretrained foundation-model weights.

The default configuration names `mistralai/Mistral-Small-4-119B-2603` as a
separately deployed executor. Operators are responsible for reviewing and complying
with the exact model card/license applicable to the model artifact they deploy.

Python dependencies are installed from their respective distributions and remain
governed by their own licenses. This proprietary repository does not relicense them.

Optional external embedding models, vector databases, web-search providers, GPU
serving engines, adapters, quantizations and datasets are not bundled and must be
independently licensed and audited before commercial use.
